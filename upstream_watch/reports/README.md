# TuringResearch Plus Upstream Reports

This directory stores generated upstream baseline and diff reports.

The first report is an initial baseline report. It cannot be used to claim that
an upstream feature is newly added.
