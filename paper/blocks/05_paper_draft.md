# TulingResearch Plus Paper Draft Block

Block kind: `paper_draft`

Required artifacts:

- `ExperimentReport`

Readiness rules:

- Blocked until `ExperimentReport` exists.
- Must include EvidenceRef entries.
- Must include required figures before the draft block can become ready.
