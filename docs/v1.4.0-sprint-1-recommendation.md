# v1.4.0 Sprint 1 Recommendation

Status: adjusted sprint plan.

Round: 291.

## Recommended Order

1. Lock Neocortica-Session production parity actions around Git context, pod
   deployment planning, dotfile handling, shell safety, and cross-platform tar
   behavior.
2. Lock Neocortica-Scholar production parity actions around MCP config,
   paper_content, paper_reference, paper_reading, SKILL.md, README tool list,
   and arxiv2md fallback policy.
3. Lock Neocortica-Web production parity actions around Apify, web_fetching,
   web_content, cache, MCP config, and dotenv removal.
4. Lock yogsoth-ai research engine production parity actions around campaign,
   vault, ontology, stress, convergence, and experiment runbook replay.
5. Build the full original repo production replay and dashboard v2 only after
   the individual production-parity workstreams are green.

## Why This Order

- It follows the latest human scan summary instead of chasing speculative ARIS
  implementation.
- It makes v1.3 fake/default parity surfaces more production-like.
- It protects the current runnable mainline.
- It keeps live networking, remote execution, and data upload disabled by
  default.

## Sprint 1 No-go

Do not implement ARIS, cross-model review, proof-checker, meta-optimize,
paper-claim-audit, default remote execution, default live networking,
automatic data upload, or any change that breaks the current runnable mainline.
