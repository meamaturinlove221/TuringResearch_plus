# TuringResearch Plus v0.2.0 Beta Test Summary

Round 62 release prep uses the Round 61 beta integration gate as the current
baseline.

## Required Test Groups

- Unit tests: pass.
- Contract tests: pass.
- Workflow fake tests: pass.
- Name integrity tests: pass.
- Package import tests: pass.
- Sprint 1 integration tests: pass.
- Sprint 2 integration tests: pass.
- Beta integration tests: pass.
- Mypy over `src`: pass.

## Beta-Specific Tests

- `tests/workflow/test_v0_2_beta_end_to_end_fake.py`
- `tests/contract/test_v0_2_beta_contracts.py`
- Handoff bundle tests under `tests/unit/test_handoff_*.py`
- Citation graph, collision, run ingest, and adapter tests

## Live Test Policy

Live tests are skipped by default. They require explicit environment opt-in and
must not fail default CI when API keys are absent.

## Evidence Safety Tests

The current suite checks that:

- no API key is required for default tests;
- default workflow tests do not use network;
- handoff bundles exclude raw data and SMPL-X model files;
- fake results are not promoted to observed;
- SparseConv3D success is not claimed without evidence.
