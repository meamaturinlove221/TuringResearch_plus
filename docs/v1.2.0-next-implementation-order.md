# v1.2.0 Next Implementation Order

Status: recommended order after Sprint 1 gate.

Round: 235.

The next implementation order follows original-reference parity first. It does
not include ARIS features and does not depend on claiming upstream file changes
from the unresolved Round 234 scan.

## Recommended Order

1. Neocortica Session parity gate:
   - Pod lifecycle preflight;
   - context package transfer policy;
   - return metadata verification;
   - shell/path/archive safety.
2. Neocortica Scholar and MCP config parity:
   - `.mcp.example.json` env block clarity;
   - scholar source fallback policy;
   - citation status and MCP test result docs.
3. Neocortica Web parity:
   - web fetching/content fake-default boundary;
   - Apify opt-in template planning;
   - live tests skipped by default.
4. yogsoth campaign / vault / ontology parity:
   - campaign catalog gate;
   - research catalog alignment;
   - vault graph / ontology SOP tests.
5. yogsoth stress-test / experiment-execution parity:
   - claim stress checks;
   - route hard gates;
   - no automatic real experiment execution.
6. Public demo refresh:
   - broaden public-safe examples;
   - keep demo/fake status explicit;
   - keep no private data and no unsupported claims.
7. v1.2 full regression:
   - v1.0 + v1.1 + v1.2 surfaces;
   - name integrity;
   - privacy/security;
   - upstream-diff honesty.
8. v1.3 ARIS study roadmap:
   - study only;
   - no implementation in v1.2.

## Blocked Until Future Diff

Any implementation that depends on a precise upstream changed file, changed
module, or new commit remains blocked until a future strict diff resolves
against `upstream_watch/baselines/v1.2_strict_baseline.json`.
