# v1.0.0 Benchmark Refresh Report

Status: refresh complete.

Round: 180.

This report refreshes benchmark/replay declarations for the v1.0 public demo.
It does not run real experiments, execute VGGT, execute Modal, or produce
observed evidence.

## Refreshed Scenario

- `examples/benchmarks/v1_public_demo_replay.yaml`

The scenario checks:

- public demo root files;
- `QUICKSTART.md`;
- `demo_manifest.yaml`;
- dashboard outputs;
- advisor bundle outputs;
- paper / related work demo outputs;
- evidence ledgers for all public demo projects.

## Existing Replay Alignment

Existing replay fixtures remain aligned:

- `examples/benchmarks/public_demo_replay.yaml`
- `examples/benchmarks/vggt_fake_replay.yaml`
- `examples/vggt-human-prior-survey/dogfooding_replay/`

VGGT fake replay remains replay-only. It does not claim new results or
SparseConv3D success.

## Expected Result

The v1 public demo replay should produce a benchmark report with:

- `scenario_id: v1_public_demo_replay`
- `status: pass`
- no missing outputs
- demo-only warnings
- no regression flags

## Safety Boundary

- no network access;
- no real experiment execution;
- no raw data;
- no private local paths;
- no restricted model payloads;
- no fake result marked observed.

## Decision

Benchmark refresh status: `pass-with-review`.
