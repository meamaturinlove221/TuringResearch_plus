# TulingResearch Plus v0.2.0 Sprint 1 Test Plan

Sprint 1 tests must be local, deterministic, and fake-mode by default. They must
not require network access, API keys, or real VGGT execution.

## Required Tests By Item

| Item | Required tests |
| --- | --- |
| Artifact Auditor | zip validity fixture; sha256 fixture; manifest completeness; missing `.npz`; candidate predictions discovery; sidecar consistency; local scan versus uploaded bundle diff; no-write-into-linked-project regression. |
| Visual Evidence Auditor | proxy heatmap classification; mask/delta board classification; full pointcloud proxy classification; true region pointcloud closeup classification; unknown visual artifact requires-human-review; advisor readiness summary gate. |
| VGGT/SMPL-X Evidence Ledger | status enum validation; V770/V129/V260/V900/V930/V999/V120/V121 ledger rows; observed versus local-observed separation; review-ready-proxy is not promotion; missing evidence requires-human-review. |
| Advisor Pack Builder | required sections present; no promotion wording; missing visual inventory blocks readiness; failures and next steps included; pack references artifact/evidence ledger entries. |
| PDF Phase B Figure/Table Extraction | local fixture PDF figure extraction; local fixture table extraction; page provenance; warning serialization; method-card draft from figure/caption metadata without fabricated claims. |

## Focused Validation Commands

Planned commands once Sprint 1 implementation begins:

```powershell
python -m pytest tests/workflow/test_vggt_local_reader_dry_run.py
python -m pytest tests/contract/test_release_gate_contract.py tests/contract/test_skills_integrity.py
python -m pytest tests/unit
python -m mypy src
```

Ruff should also run when available in the active Python environment.

## Fixtures

| Fixture | Status | Notes |
| --- | --- | --- |
| Tiny zip bundle | planned | Contains manifest, sidecar, json/csv/png/npz placeholders. |
| Broken zip bundle | planned | Exercises zip test failure path. |
| Missing npz bundle | planned | Exercises missing artifact detection. |
| Visual board fixtures | planned | Small PNG/JPG files with filenames and sidecars identifying evidence type. |
| Tiny PDF fixture | planned | Supports figure/table extraction without private papers. |
| Modal-like sidecar fixture | planned | Fake local fixture only; no Modal execution. |

