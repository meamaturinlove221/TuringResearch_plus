# v0.3.0 Sprint 2 Test Plan

Status: scope locked.

## Web Fetch / Apify

- fake web fetch returns deterministic content;
- default tests do not access network;
- missing `APIFY_TOKEN` does not fail default tests;
- source hygiene blocks restricted content;
- source metadata includes URL, retrieval time, and content hash;
- live tests are marked and skipped by default.

## Related Work Positioning

- positioning report consumes method cards, citation graph, and collision risk;
- unsafe claims are rejected;
- missing evidence is surfaced;
- markdown and JSON export work;
- generated positioning remains `requires_human_review`.

## Skill ENTRY / Routing

- `ENTRY.md` exists;
- every route points to an existing skill or lane;
- no stale skill names;
- missing route targets are reported.

## Vault Graph Enhancement

- typed edge traversal works;
- duplicate or unsupported edges are rejected;
- edge audit reports missing evidence;
- graph export remains deterministic.

## Ontology SOPs

- seed concept search SOP fixture;
- source gathering SOP fixture;
- concept page creation SOP fixture;
- alias resolution SOP fixture;
- edge batch creation SOP fixture;
- hierarchy visualization SOP fixture;
- gap detection SOP fixture;
- merge candidates SOP fixture;
- confidence update SOP fixture;
- ontology export SOP fixture.
