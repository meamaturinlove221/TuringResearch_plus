# v1.3.0 Full Regression Report

Status: full regression passed with review.

Round: 288.

This report records the v1.3 full regression gate across the v1.2 baseline and
the v1.3 full original-reference parity additions.

## Regression Decision

Decision: `PASS WITH REVIEW`.

The v1.3 release-candidate surface is coherent enough for human release review.
It is not released, not tagged, and not published. Human review is still
required before any release branch push, GitHub release, package publication,
or child repository creation.

## Coverage

| Surface | Regression coverage |
| --- | --- |
| v1.2 workflows | v1.2 full fake replay, release contracts, public demo, security/privacy, and reference parity surfaces remain present. |
| session runtime parity | Preflight, context pack, fake transfer, return verifier, workflow replay, and dashboard remain fake/default and review-only. |
| scholar/web parity | Scholar tools, Web tools, Apify templates, and MCP mapping remain fake/default with live providers disabled by default. |
| yogsoth parity | Campaign trace, Research Catalog dashboard, vault wiki demo, ontology demo, stress scenario library, and convergence report remain displayable and testable. |
| original parity demo | v1.3 public demo remains fake/demo-only and public-safe. |
| security/privacy | v1.3 security, privacy, and secret scan gates pass with review. |
| ARIS deferred | ARIS remains reference-only and outside v1.3 implementation. |

## Verification

| Check | Result |
| --- | --- |
| Full pytest | passed |
| `python -m mypy src` | passed |
| Name integrity | passed |
| Privacy gate | passed |
| v1.3 release contracts | passed |
| v1.3 full original parity replay | passed |
| v1.3 original parity public demo | passed |
| v1.3 security/privacy gate | passed |
| Pre-push scan | passed with documented non-blocking safety wording |

## Fixed During Regression

- Restored README wording required by legacy public launch tests:
  `not proof that any VGGT experiment succeeded`.
- Added an explicit negative boundary that TuringResearch does not claim VGGT or
  SparseConv3D experiment success.

## Safety Notes

- No network access was required for the public/fake regression path.
- No automatic GitHub release was created.
- No automatic PyPI publish was added.
- No automatic child repository creation was performed.
- No live tests were enabled by default.
- No automatic experiment execution, remote command execution, Modal call, GPU
  call, unknown plugin execution, or ARIS runtime feature was added.
- No private data, raw data, private local paths, or restricted model payloads
  are included in the v1.3 public demo surfaces.
- Fake/demo outputs remain fake/demo and must not be presented as observed
  evidence.
- SparseConv3D is not claimed as successful.

## Carryover Items

- The current working branch still needs maintainer-managed branch cleanup.
- Public release remains a human action.
- Optional live provider and live SSH/SFTP behavior remains disabled by default.
- ARIS study remains deferred to a separate scope lock and safety review.
