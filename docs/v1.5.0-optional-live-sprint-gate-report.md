# v1.5.0 Optional Live Sprint Gate Report

Status: gate passed.

Round: 349.

This report closes the optional live polish sprint from Round 344 through Round
348. It records documentation, examples, skipped-live tests, env policy tests,
and the unified live safety gate. It does not run live providers, open network
connections, connect to remote hosts, or require credentials.

## Gate Decision

Decision: `GO FOR OPTIONAL LIVE POLISH / NO-GO FOR DEFAULT LIVE`.

Optional live surfaces are ready for private human opt-in documentation review.
They are not enabled by default and are not part of the default test suite.

## Sprint Inputs

| Round | Surface | Result |
| --- | --- | --- |
| 344 | optional live scope | pass |
| 345 | Scholar live optional guide | pass |
| 346 | Web / Apify live optional guide | pass |
| 347 | SFTP live optional guide | pass |
| 348 | unified optional live safety gate | pass |

## Gate Checks

| Check | Result | Evidence |
| --- | --- | --- |
| Scholar live optional | pass | `docs/scholar-live-optional-guide.md`, Scholar env policy test |
| Web / Apify live optional | pass | `docs/web-apify-live-optional-guide.md`, Apify env policy test |
| SFTP live optional | pass | `docs/sftp-live-optional-guide.md`, SFTP env policy test |
| MCP env block | pass | `.mcp.example.json` keeps live flags disabled and credentials blank |
| live tests skipped by default | pass | `pyproject.toml` excludes `live` and `manual` markers |
| no secrets | pass | committed examples use blank placeholders only |
| no remote command | pass | SFTP guide and safety gate block command execution |
| no private scraping | pass | Web / Apify guide blocks private scraping and login bypass |
| no old naming | pass | name integrity gate remains required |

## Required Defaults

```text
TURINGRESEARCH_MODE=fake
TURINGRESEARCH_ENABLE_LIVE_TESTS=0
TURINGRESEARCH_ENABLE_SEMANTIC_SCHOLAR_LIVE=0
TURINGRESEARCH_ENABLE_WEB_LIVE=0
TURINGRESEARCH_ENABLE_APIFY_LIVE=0
TURINGRESEARCH_ENABLE_SFTP_LIVE=0
SEMANTIC_SCHOLAR_API_KEY=<blank>
APIFY_TOKEN=<blank>

TURINGRESEARCH_SFTP_CREDENTIAL=<blank>
```

## NO-GO Items

- default live networking;
- default SSH or SFTP;
- remote command execution;
- remote delete;
- private scraping;
- login bypass;
- paywall bypass;
- committed secrets;
- token logging;
- automatic Evidence Ledger write;
- live output treated as observed evidence without human review.

## Human Review Notes

Optional live polish is documentation and test-policy readiness. Any future live
run must be explicitly requested by a human, configured outside the repository,
and reviewed separately. This sprint is not proof that any provider call,
transfer, or remote access succeeded.
