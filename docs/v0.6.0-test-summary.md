# v0.6.0 Test Summary

Status: Round 125 verification passed.

## Required Checks

| Check | Result | Notes |
| --- | --- | --- |
| Full pytest | PASS | `python -m pytest -q` completed successfully. |
| mypy `src` | PASS | `python -m mypy src` completed successfully. |
| Version import check | PASS | `turing_research` and `turing_research_plus` report `0.6.0rc0`. |
| Name integrity | PASS | `tests/contract/test_name_integrity.py` passed. |
| Privacy gate | PASS | Focused privacy gate tests passed. |
| Quality regression gate | PASS | Focused quality regression gate tests passed. |
| Pre-push safety scan | PASS WITH REVIEW | Round 125 files contain no real secrets, private paths, raw payloads, model files, or large files. |

## Expected Commands

```powershell
python -m pytest -q
python -m mypy src
python -m pytest tests/contract/test_package_imports.py tests/contract/test_public_import_surface.py -q
python -m pytest tests/contract/test_name_integrity.py -q
python -m pytest tests/unit/test_privacy_models.py tests/unit/test_privacy_policy.py tests/unit/test_privacy_scanner.py tests/unit/test_redaction_rules.py tests/unit/test_privacy_report.py tests/workflow/test_public_demo_privacy_gate.py -q
python -m pytest tests/unit/test_quality_metrics.py tests/unit/test_regression_gate.py tests/unit/test_quality_report.py tests/workflow/test_quality_gate_public_demo.py -q
```

## Current Boundaries Under Test

- Default tests do not require network access.
- Live tests remain optional and skipped by default.
- Fake/demo outputs are not promoted to observed evidence.
- Paper assembly remains blocked where real citations or real experiment
  results are missing.
- Plugin and MCP plugin registries remain manifest-only.
- Privacy reports do not delete or overwrite files.
- SparseConv3D success is not claimed.

## Round 125 Notes

The release prep version is `0.6.0rc0`. This is release candidate preparation,
not a PyPI publication, tag, or GitHub release.
