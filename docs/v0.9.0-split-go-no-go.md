# v0.9.0 Split Go / No-Go

Status: gate decision.

Round: 172.

## Decision

Final human review: `GO`.

Automatic physical repository split: `NO-GO`.

NO-GO FOR AUTOMATIC PHYSICAL SPLIT.

GitHub repository creation: `NO-GO`.

External push: `NO-GO`.

## Why Human Review Can Proceed

- VGGT case dry-run export exists and has no release blocker.
- Examples dry-run export exists and has no release blocker.
- README files are clear and point back to the flagship.
- Main repo strategy preserves install path and quickstart.
- Dry-run exports contain no secrets, raw data, SMPL-X payloads, private paths,
  or unsupported success claims.

## Why Physical Split Is Still No-Go

- Maintainer approval is still required.
- Final external repository names and ownership are not confirmed.
- Exact release branch and export process are not approved.
- Public launch timing is not approved.
- This round is explicitly dry-run and local-only.

## Next Gate

If maintainers want to proceed, the next round should be a controlled split
approval gate, not an automatic repository creation step.
