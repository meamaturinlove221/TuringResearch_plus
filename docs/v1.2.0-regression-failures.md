# v1.2.0 Regression Failures

Status: no unresolved failures.

Round: 255.

This file records failures found during the v1.2 full regression gate.

## Current Failure List

- none.

## Fixed During Round 255

- none required after the final v1.2 regression gate passed.

## Non-failure Policy Wording

The following phrases may appear as non-blocking safety policy text:

- fake/demo output must not be presented as verified live research work;
- raw data must not be included;
- private paths must not be included;
- restricted model payloads must not be included;
- live adapters must remain opt-in;
- no automatic GitHub release;
- no automatic PyPI publish;
- no automatic child repository creation;
- ARIS deferred.

These are safety boundaries, not leaks, release actions, ARIS adoption claims,
or verified live research claims.

## Fix Policy

If a future failure appears, fix only the failing regression surface. Do not
introduce new features, enable live tests by default, publish packages, create
repositories, enable remote execution, or turn demo output into verified live
research work during a regression round.
