# v0.3 Sprint 2 Integration Report

Status: integration gate complete / GO WITH REVIEW.

Round 76 checks the v0.3 Sprint 2 fake/default integration chain across Web
Fetch, Apify, Related Work Positioning, Skill Routing, Vault Graph, Ontology
SOPs, and the VGGT Research Knowledge Pack.

## Scope Checked

- Web Fetch Adapter / Apify Adapter.
- Related Work Positioning Generator.
- Skill ENTRY / Routing Table.
- Vault Graph Enhancement.
- Ontology SOPs.
- VGGT Research Knowledge Pack.

## A. Web Flow

`local HTML fixture -> WebFetchResult -> content cache -> PaperMethodCard /
RelatedWorkPositioning input`

Result: pass.

- Local HTML fixture mode returns `WebFetchResult` without network access.
- Cache hit/miss behavior works with the local in-memory cache.
- Source metadata preserves `source_url`, retrieval time, content hash, and
  `human_verified=false`.
- Web output can feed method-card extraction and related-work positioning as
  review input.

## B. Related Work Flow

`CitationGraph + PaperMethodCards + CollisionRisk -> RelatedWorkPositioningReport
-> safe claims / unsafe claims`

Result: pass.

- Related-work reports remain conservative.
- Safe claims describe SMPL-X feature encoding and inspiration sources only.
- Unsafe claims keep definitive no-collision, complete review, and SparseConv3D
  success out of usable related-work language.
- HART, VGGT-HPE, HGGT, Fus3D, and other close papers remain review-required.

## C. Skill Routing Flow

`task description -> SkillRoutingDecision -> recommended turingresearch-* skill
-> .agents/ENTRY.md consistency`

Result: pass.

- Routing returns ranked `turingresearch-*` recommendations.
- Unknown tasks fall back to `turingresearch-master-orchestrator`.
- Routing is recommendation-only and does not execute skills or tools.
- `.agents/ENTRY.md`, `.agents/ROUTING_TABLE.md`, and registry behavior align.

## D. Vault Graph Flow

`Evidence / Paper / Method / Route / Failure -> VaultGraph -> EdgeAuditReport ->
optional Wikilink markdown export`

Result: pass.

- Vault graph serializes as JSON.
- Edge audit reports evidence-bearing edges without source refs.
- Low-confidence and requires-human-review nodes stay visible.
- `[[wikilink]]` export works as navigation, not proof.

## E. Knowledge Pack Flow

`Evidence + Artifact + Visual + Failure + Route + RelatedWork + VaultGraph ->
VGGT Research Knowledge Pack`

Result: pass.

- The pack includes north star, current state, evidence, artifact, visual,
  failure, route, related-work, method taxonomy, vault graph, advisor brief,
  next actions, and manifest.
- V260 remains `hard-blocked`.
- V999-SparseConv3D remains `not-enough-evidence`.
- V120 / V121 remain `requires-human-review`.
- Modal SparseConv3D remains `planned / requires-real-experiment`.

## Required Gate Checks

| Gate | Result |
| --- | --- |
| Default tests do not require network | pass |
| Live web / Apify tests skipped by default | pass |
| No API key required | pass |
| No paywall bypass | pass |
| No raw data ingestion | pass |
| No SMPL-X model packaging | pass |
| No fake result marked observed | pass |
| No legacy project naming | pass |
| Related work remains conservative | pass |
| Ontology graph requires human review | pass |

## Validation Run

- `python -m pytest tests/workflow -q`: pass.
- `python -m pytest tests/contract -q`: pass.
- `python -m pytest tests/unit -q`: pass.
- `python -m pytest tests/contract/test_name_integrity.py tests/contract/test_package_imports.py tests/contract/test_public_import_surface.py -q`: pass.
- `python -m mypy src`: pass.
- `python -m pytest -q`: pass.
- Focused `ruff check` on new and touched Round 76 tests: pass.
- Focused old-name scan on Round 76 files: no hit.
- Focused secret/private-path scan on Round 76 files: no real key, token, private path, or SMPL-X model file hit.

## Recommendation

GO WITH REVIEW for v0.3 Sprint 2 fake/default workflows. This is not a release,
not a live-web claim, not a final paper conclusion, and not a VGGT experiment
result.
