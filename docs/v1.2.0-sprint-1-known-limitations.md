# v1.2.0 Sprint 1 Known Limitations

Status: active limitations.

Round: 235.

## Limitations

- The v1.2 strict upstream diff created a first machine baseline, but all
  configured repositories were unresolved due to public metadata rate limiting.
- No upstream added, modified, or deleted files are known from Round 234.
- Parity target rounds are tentative windows, not proof of upstream changes.
- ARIS is intentionally deferred and must not enter v1.2 implementation.
- Neocortica Session parity remains local safety and handoff planning only; it
  is not remote execution orchestration.
- Neocortica Scholar parity does not add MinerU or heavy PDF fallback runtime.
- Neocortica Web parity does not enable live web or Apify access by default.
- yogsoth parity does not introduce an autonomous agent runtime.
- The current worktree and branch state still require human cleanup before any
  push or release branch operation.

## Risk Controls

- Keep fake/live boundaries visible in every parity implementation.
- Require name integrity after docs updates.
- Require privacy/security gates before any public-facing release candidate.
- Require a resolved strict diff before claiming new upstream deltas.
- Keep ARIS study docs separate from v1.2 implementation docs.
