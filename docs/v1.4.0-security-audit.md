# v1.4.0 Security Audit

Status: PASS WITH REVIEW.

Round: 325.

This is a local security audit for the v1.4 original repo production parity
surface. It is not a penetration test and not a certification.

## Scope

- Session production parity surfaces;
- Scholar production parity surfaces;
- Web production parity surfaces;
- yogsoth production parity E2E surfaces;
- Original Repo Parity Dashboard v2;
- README / docs production polish;
- ARIS deferral docs.

## Checks

| Check | Result | Notes |
| --- | --- | --- |
| secrets | pass | No real token-like values in v1.4 surfaces. |
| API key values | pass | Public docs only describe fake/live boundaries. |
| `.env` files | pass with historical fixture allowlist | No new v1.4 `.env` file is included. |
| raw data | pass | v1.4 demos are fake/demo-only. |
| restricted model payloads | pass | No restricted model payload is included. |
| unsafe remote execution | pass | Remote command execution remains disabled or rejected by default. |
| default live SSH/SFTP | pass | Live SSH/SFTP remains opt-in or skipped by default. |
| paywall bypass | pass | Scholar/Web docs reject paywall bypass. |
| old naming | pass | Name integrity tests pass. |

## Security Boundary

The v1.4 production parity work adds fake/default operator paths, E2E demos,
dashboards, docs, and review gates. It does not add default network access,
remote command execution, live SSH/SFTP by default, unknown plugin execution,
automatic experiment execution, automatic Evidence Ledger mutation, or ARIS
runtime features.

## Remaining Review Items

- Pattern-based scans can miss context-specific leaks.
- Live provider and live SSH/SFTP paths remain private opt-in surfaces and need
  separate review before use.
- Human review remains required before any public release.

## Decision

`PASS WITH REVIEW`
