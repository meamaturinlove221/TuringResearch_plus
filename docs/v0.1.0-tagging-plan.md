# TulingResearch Plus v0.1.0 Tagging Plan

This is a manual tagging plan. It must not be run automatically.

## Version Sources

- `VERSION`: `0.1.0`
- `pyproject.toml`: `version = "0.1.0"`
- `src/tuling_research/__init__.py`: `__version__ = "0.1.0"`
- `src/tuling_research_plus/__init__.py`: `__version__ = "0.1.0"`
- `CHANGELOG.md`: `[0.1.0] - 2026-05-20`

## Release Branch

Suggested branch name:

```powershell
release/v0.1.0
```

Branch creation is optional and should be done only by a maintainer.

## Tag

Suggested tag:

```powershell
v0.1.0
```

Suggested local command after human approval:

```powershell
git tag -a v0.1.0 -m "TulingResearch Plus v0.1.0"
```

Do not run this command until the repository is initialized, all release gates pass, and the maintainer explicitly approves tagging.

## Release Title

```text
TulingResearch Plus v0.1.0
```

## Release Description Source

Use:

```text
docs/release-notes-v0.1.0.md
```

## Do Not

- Do not push the tag automatically.
- Do not create a remote release automatically.
- Do not publish to PyPI automatically.
- Do not change public API during tagging.
- Do not add features during tagging.

## Manual Push Command

Only after explicit maintainer approval:

```powershell
git push origin v0.1.0
```
