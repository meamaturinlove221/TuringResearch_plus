# v1.0.0 Full Regression Report

Status: full regression passed.

Round: 194.

This report records the v1.0 full regression gate across API, CLI/MCP,
quickstart, demo, dashboard/export, privacy, plugin safety, split readiness,
and public launch release-candidate surfaces.

## Coverage

| Surface | Coverage |
| --- | --- |
| API freeze | `contracts/v1_public_api.yaml`, public API tests |
| Namespace compatibility | legacy and new facade import tests |
| CLI/MCP sanity | package scripts and `.mcp.example.json` fake defaults |
| Quickstart | v1 public quickstart fake workflow |
| Public demo | public demo refresh and replay |
| Security/privacy | v1 security/privacy gate and privacy scanner |
| Plugin safety | sandbox/permission/compatibility tests |
| Dashboard/export | optional PDF/PPTX and export quality gate |
| Split readiness | local `split_ready/` bundles and split go/no-go docs |
| Public launch RC | README/public launch RC tests |

## Regression Decision

Decision: `PASS WITH REVIEW`.

The v1.0 release candidate regression surface is clean enough for release
candidate review. Human review is still required before publishing, tagging,
or pushing release branches.

## Verification

| Check | Result |
| --- | --- |
| Full pytest | passed |
| `python -m mypy src` | passed |
| Name integrity | passed |
| Privacy gate | passed |
| v1 full fake replay | passed |
| v1 release contracts | passed |
| Pre-push scan | passed with documented non-blocking policy wording |

## Fixes During This Round

- Adjusted the full fake replay privacy check so `examples/public_demo/` must
  be scanner-clean, while `split_ready/` is validated through its explicit
  safety reports. This avoids treating policy text such as "raw data is
  excluded" as a payload leak.

## Boundaries

- No network access.
- No release creation.
- No external repository push.
- No real experiment execution.
- No final paper conclusion generation.
- No physical split execution.
