# v0.5.0 Alpha Integration Report

Status: integration gate passed for fake/default workflows.

Round: 98.

## Scope

This integration gate covers v0.5 alpha additions:

- Lightweight Dashboard UI
- Advisor PDF / PPTX Export Plan
- Project Template Generator
- MCP / CLI Packaging Polish
- Public Demo Suite
- VGGT Dogfooding Replay

No live services, remote commands, VGGT experiments, Modal runs, PDF/PPTX binary
converters, or publish steps were executed.

## A. Demo Suite

Checked flow:

`examples/public_demo -> evidence / artifact / visual / related work / advisor / dashboard examples`

Result: pass.

The public demo is fake/demo only and contains no private paths, raw data,
private model files, or API keys.

## B. Dashboard

Checked flow:

`research_knowledge_pack + advisor_pack + run_dashboard -> StaticDashboardSpec -> HTML / Markdown`

Result: pass.

The generated dashboard remains static and review-only. It does not run VGGT,
Modal, a server, login, cloud deployment, or live refresh.

## C. Advisor Export

Checked flow:

`AdvisorMarkdownBundle -> AdvisorPdfExportPlan / AdvisorPptxExportPlan -> AdvisorExportManifest`

Result: pass.

The export layer creates plan files only. No real PDF or PPTX is generated and
no external converter is called.

## D. Project Template

Checked flow:

`ProjectTemplateRequest -> ProjectTemplateResult -> generated skeleton`

Result: pass.

The generated project skeleton includes README, docs, lanes, examples,
contracts, and feature capsule folders. It contains no observed evidence.

## E. Packaging

Checked flow:

`pyproject.toml + .env.example + .mcp.example.json + docs -> packaging contract tests`

Result: pass.

Package name, import package names, CLI entrypoints, MCP entrypoint, fake-default
behavior, and no-secret examples are checked.

## F. VGGT Replay

Checked flow:

`research intent -> evidence ledger -> artifact audit -> visual audit -> run ingest -> failure taxonomy -> route DSL -> related work -> vault graph -> advisor pack -> dashboard -> next action`

Result: pass.

Replay is review-only and does not add new experiment evidence. SparseConv3D
success remains not established.

SparseConv3D success remains not established by v0.5 alpha integration.

## Gate Checks

- Demo suite can be inspected locally.
- Dashboard can be generated from existing local review artifacts.
- Advisor export plan can be generated without binary converters.
- Project template can be generated in a temporary directory.
- Packaging tests pass.
- No legacy project naming in Round 98 files.
- No secrets in Round 98 files.
- No raw data in Round 98 files.
- No fake result is marked observed.

## Result

GO WITH REVIEW.

v0.5 alpha fake/default integration is coherent and suitable for maintainer
review. It is not a public release, not a real VGGT replay result, and not a
binary export implementation.
