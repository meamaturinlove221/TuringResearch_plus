# v1.2.0 Handoff

Status: final handoff.

Round: 258.

This handoff is for the next human reviewer or future development session. It
summarizes what is ready, what is not ready, and what requires human approval.

## Branch Status

Current local branch observed during Round 258:

- current branch: `pre-rename-round38-checkpoint`;
- local `dev/mainline`: present;
- local `release/v1.2.0-rc`: not present.

Because the working tree is historically dirty, a human should create or check
out a clean release branch before any v1.2 publication action.

## Original Reference Parity Status

Original reference parity is ready for human review.

- Neocortica Session / Scholar / Web parity: pass with deferred gaps.
- MCP config parity: pass.
- Skill SOP parity: pass.
- yogsoth campaign / vault / ontology / stress-test / experiment execution
  parity: go with review.
- Research Catalog integration: complete.

Deferred or rejected items remain explicitly out of release scope:

- MinerU and heavy PDF fallback;
- remote execution orchestration;
- SSH, tmux, and provision workflows;
- unknown remote execution;
- automatic experiment execution.

## ARIS Status

ARIS remains deferred from v1.2.

v1.3 study preparation is ready:

- `docs/v1.3.0-aris-study-roadmap.md`;
- `docs/v1.3.0-aris-feature-evaluation-matrix.md`;
- ARIS risk register;
- `docs/v1.3.0-aris-non-goals.md`;
- `docs/v1.3.0-sprint-1-recommendation.md`.

v1.3 should start with study / matrix / small fake prototype only. It should
not start with a full ARIS merge.

## Test Status

Most recent recorded v1.2 gates:

- full pytest: passed in Round 255;
- `python -m mypy src`: passed in Round 255;
- name integrity: passed in Round 256 and Round 257 checks;
- privacy/security gate: passed with review in Round 254;
- v1.2 full fake replay: passed in Round 250 and included in regression;
- v1.2 release contracts: passed in Round 255 and Round 256 checks;
- public demo v1.2: passed in Round 252.

Round 258 final smoke is a handoff check, not a replacement for clean release
branch CI.

## Public Demo Status

The v1.2 public demo is fake/demo-only and intended to run without:

- API keys;
- private data;
- raw data;
- restricted model payloads;
- default network access;
- real experiment execution.

Demo entry:

- `examples/public_demo/v1_2_demo/README.md`;
- `docs/v1.2.0-public-demo-refresh.md`;
- `docs/reference-parity-dashboard.md`.

## Release Status

v1.2 release docs are drafted:

- release notes;
- feature list;
- known limitations;
- test summary;
- upgrade guide;
- GitHub release draft;
- changelog;
- version metadata: `1.2.0rc0`.

No tag, GitHub release, PyPI package, child repository, or live provider action
has been created.

## Next Human Actions

1. Review README and v1.2 release notes together.
2. Review v1.2 public demo and Reference Parity Dashboard.
3. Review security/privacy reports and secret scan report.
4. Create or check out a clean `release/v1.2.0-rc` branch if release is
   approved.
5. Re-run full tests and `python -m mypy src` on that clean branch.
6. Confirm whether to tag.
7. Confirm whether to create a GitHub release.
8. Decide whether v1.3 ARIS study starts with docs-only or a small fake
   prototype.

## Hard Stops

Do not launch if any of these are true:

- tests fail on the clean release branch;
- mypy fails without documented exception;
- name integrity fails;
- security/privacy gate fails;
- public demo includes private data;
- fake/demo output is presented as verified live research work;
- ARIS study items are presented as v1.2 implementation;
- release branch includes unrelated historical work;
- maintainer approval is missing.
