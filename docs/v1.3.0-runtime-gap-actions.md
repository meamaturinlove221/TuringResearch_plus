# v1.3.0 Runtime Gap Actions

Status: proposed.

Round: 260.

## Priority 1: Session Runtime Replay

Goal: turn Session parity from scattered fake paths into one coherent local
runtime replay.

Actions:

1. Build a fake-first session runtime path:
   context pack -> preflight -> transfer placeholder -> structured return ->
   verifier -> report.
2. Keep remote execution disabled.
3. Keep returned evidence updates proposed-only.
4. Add checksum and unsafe-file blocking before any ingest proposal.

Target status after action: `fake-runnable`.

## Priority 2: Scholar/Web Tool Surface Replay

Goal: make Scholar and Web parity feel like usable tools without enabling live
networking by default.

Actions:

1. Add fake Scholar query workflow with source priority and fallback report.
2. Add fake Web fetch/content workflow with source metadata and cache status.
3. Add no-key optional-provider reports for live surfaces.
4. Keep MinerU, paywall bypass, and default networking out.

Target status after action: `fake-runnable`.

## Priority 3: Research Catalog Execution Trace

Goal: prove the catalog can route a public-safe task through the main local
review surfaces.

Actions:

1. Task description -> campaign route.
2. Campaign route -> skill map.
3. Skill map -> vault/ontology/stress/runbook steps.
4. Produce one trace artifact for the parity dashboard.
5. Keep no-agent-runtime and no-network guarantees.

Target status after action: `fake-runnable`.

## Priority 4: Runtime-Aware Public Parity Dashboard

Goal: show runtime truth in the public/demo surface.

Actions:

1. Add runtime status category per parity area.
2. Link each area to test evidence and example fixtures.
3. Separate `runnable` from `fake-runnable`.
4. Keep ARIS in deferred study.

Target status after action: `runnable` dashboard generation with
`fake-runnable` entries where appropriate.

## Priority 5: v1.3 Full Original Parity Replay

Goal: replace isolated parity tests with a full safe replay.

Actions:

1. Run Session runtime replay.
2. Run Scholar/Web fake tool surface replay.
3. Run campaign/vault/ontology/stress/runbook trace.
4. Render runtime-aware parity dashboard.
5. Run security/privacy/name gates.

Target status after action: `PASS WITH REVIEW`.

## Explicit Non-Actions

- Do not implement ARIS.
- Do not implement default SSH/SFTP.
- Do not run remote commands.
- Do not enable default live networking.
- Do not auto-run experiments.
- Do not write observed results from fake/demo output.
- Do not create child repositories automatically.
