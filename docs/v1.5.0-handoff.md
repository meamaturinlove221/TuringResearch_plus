# v1.5.0 Handoff

Status: final handoff.

Round: 360.

This handoff is for the next human reviewer or future development session. It
summarizes what is ready, what is not ready, and what requires human approval.

## Branch Status

Current local branch during Round 360:

- current branch: `pre-rename-round38-checkpoint`;
- local `dev/mainline`: present;
- local `release/v1.5.0-rc`: not present.

Because the working tree is historically dirty, a human should create or check
out a clean release branch before any v1.5 publication action.

## Docs Deployment Dry-run Status

Docs deployment prep is ready for human review.

- Strategy, GitHub Pages-ready planning, static hosting comparison, local-only
  fallback, and no-deploy bundle planning are documented.
- Docs-site build hardening reports broken links, missing pages, orphan pages,
  nav validation, manifest output, and private-path scan status.
- Public navigation is polished around Overview, Quickstart, Concepts,
  Original Repo Parity, Public Demo, Docs / Dashboard, Plugin / MCP, Split
  Repos, Security / Privacy, FAQ, and Roadmap.
- The deployment dry-run package exists in `docs-site/dist/` with a public-safe
  manifest at `docs-site/dist_manifest.yaml`.

This is not a public deployment. No real public URL, analytics, or private file
upload is part of v1.5.

## Split Manual Pack Status

Split manual packs are ready for human review.

- `split_manual/turingresearch-vggt-case/` is prepared as a manual pack.
- `split_manual/turingresearch-examples/` is prepared as a manual pack.
- Git init dry-run notes and release checklists are present.
- The main repository remains the flagship install, docs, release, public API,
  and star entry.

No child repository was created automatically. No external remote was pushed.
No real child-repo URL was written.

## Optional Live Polish Status

Optional live polish is documented and safety-gated.

- Scholar live remains optional and disabled by default.
- Web / Apify live remains optional and disabled by default.
- SFTP live remains optional and disabled by default.
- Live tests are skipped by default.
- Credentials are environment-only and must stay outside the repository.

No live provider was run, no SSH/SFTP connection was opened, and no remote
command was executed.

## Dashboard Showcase Status

Dashboard showcase is ready for local/static review.

- Landing page: `examples/public_demo/dashboard_showcase/landing.html`.
- Parity showcase view: `examples/public_demo/dashboard_showcase/parity.html`.
- Interview demo view: `examples/public_demo/dashboard_showcase/interview.html`.

The showcase is static/local-first. It is not deployed, does not use analytics,
does not load external assets, and does not call live providers.

## Release Status

v1.5 release docs are drafted:

- release notes;
- feature list;
- known limitations;
- test summary;
- upgrade guide;
- GitHub release draft;
- release checklist;
- post-release verification plan;
- changelog entry;
- version metadata: `1.5.0rc0`.

No tag, GitHub release, PyPI package, docs deployment, child repository, or live
provider action has been created.

## ARIS Status

ARIS remains deferred from v1.5.

v1.6 planning keeps ARIS as study-only:

- `docs/v1.5.0-aris-still-deferred.md`;
- `docs/v1.6.0-aris-status.md`;
- `docs/v1.6.0-roadmap.md`.

ARIS should not start as a default implementation. Any future ARIS work needs a
separate scope lock, safety model, and regression gate.

## Test Status

Most recent recorded v1.5 gates:

- v1.5 full replay: PASS WITH REVIEW in Round 355;
- security/privacy gate: PASS WITH REVIEW in Round 356;
- release contracts and version smoke: passed in Round 357;
- GitHub release draft smoke: passed in Round 358;
- name integrity and public release hygiene: passed in Round 359 checks.

Round 360 final smoke is a handoff check, not a replacement for clean release
branch CI.

## Next Human Actions

1. Review `docs/v1.5.0-release-notes.md` and `docs/github-release-draft-v1.5.0.md`.
2. Review `docs/v1.5.0-full-replay-report.md`.
3. Review `docs/v1.5.0-security-audit.md`, `docs/v1.5.0-privacy-audit.md`, and
   `docs/v1.5.0-secret-scan-report.md`.
4. Inspect `docs-site/dist/` and `docs-site/dist_manifest.yaml`.
5. Review split manual packs under `split_manual/`.
6. Review dashboard showcase pages locally.
7. Create or check out a clean `release/v1.5.0-rc` branch if release is
   approved.
8. Re-run full tests and `python -m mypy src` on that clean branch.
9. Decide whether to tag, publish a GitHub release, publish PyPI, deploy docs,
   or create child repositories.
10. Keep ARIS deferred unless a later scope lock explicitly starts a study-only
    track.

## Hard Stops

Do not launch if any of these are true:

- tests fail on the clean release branch;
- mypy fails without documented exception;
- name integrity fails;
- security/privacy gate fails;
- docs dry-run output contains private data, raw data, secrets, or a fake URL;
- split manual packs contain private data, raw data, secrets, or unsupported
  claims;
- optional live credentials appear in repository files;
- dashboard pages imply live deployment or live results;
- fake/demo output is presented as verified live research work;
- ARIS study items are presented as v1.5 implementation;
- release branch includes unrelated historical work;
- maintainer approval is missing.
