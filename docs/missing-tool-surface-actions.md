# Missing Tool Surface Actions

Status: proposed.

Round: 261.

## Action 1: Session Runtime Tool Path

Missing surface:

- transfer runner;
- launch placeholder report;
- returned bundle verifier;
- one operator-facing session runtime report.

Required boundary:

- no default SSH;
- no tmux attach;
- no remote command execution;
- no automatic evidence ledger writes.

## Action 2: Scholar Query Tool Path

Missing surface:

- one fake/default query workflow that combines source priority, cached
  Markdown policy, paper reference fallback, reading plan, and fallback report.

Required boundary:

- no automatic full paper download;
- no paywall bypass;
- no MinerU heavy fallback in this path;
- no final paper conclusion.

## Action 3: Web Source Tool Path

Missing surface:

- one fake/default web source workflow that combines dry-run fetch, content
  review, source metadata, cache policy, and Apify optional skip behavior.

Required boundary:

- no default networking;
- no private content fetch;
- no cookie storage;
- no real provider credential in repo.

## Action 4: Research Catalog Trace Tool Path

Missing surface:

- one local trace that maps task description to campaign, skills, vault,
  ontology, stress test, experiment runbook, and advisor/public-release notes.

Required boundary:

- no multi-agent runtime;
- no LLM call;
- no automatic experiment execution;
- no fake/demo result promotion.

## Action 5: Runtime-Aware Tool Surface Dashboard

Missing surface:

- machine-readable tool surface status that separates `mcp-stdio`,
  `local-python`, `config-docs`, `policy-only`, `missing`, `deferred`, and
  `rejected`.

Required boundary:

- do not present planned tools as enabled tools;
- do not present fake/default surfaces as live provider success.
