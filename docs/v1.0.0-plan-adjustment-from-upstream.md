# v1.0.0 Plan Adjustment From Upstream

Status: plan adjustment.

Round: 175 upstream refresh.

The upstream refresh does not change the v1.0 main goal: a public, local-first,
fake/demo-first Research OS with stable public API contracts and review gates.

## Adjustments Before API Freeze

If applying this refresh before the v1.0 API freeze sequence, insert these two
planning items:

1. Campaign Catalog and MCP Config Polish.
2. Pod Context Lifecycle Safety Plan.

In the current repository state, later API/install/public-launch gates already
exist. Therefore this adjustment should be treated as a documentation and risk
alignment note, not as a reason to reopen shipped implementation scope.

## v1.0 Inclusions

Add or verify these docs before public launch review:

- TuringResearch Campaign Catalog documentation.
- Pod Context Lifecycle Safety Plan.
- Scholar Pipeline public docs and MCP config examples.
- Web adapter docs that keep live mode optional.
- Release safety note for pod context transfer and context return metadata.

## v1.0 Exclusions

The following must remain out of v1.0:

- MinerU / heavy PDF fallback;
- remote execution orchestration;
- Apify workflow templates;
- default live networking;
- copied upstream implementation code.

## Risk Adjustment

The highest new risk is not feature absence; it is messaging drift. Docs must
avoid making upstream ideas sound implemented locally.

Mitigation:

- mark upstream-derived items as planning signals;
- keep v1.1/v1.2 candidates separate;
- keep fake/live and planned/observed boundaries explicit;
- require human review before any release claim.
