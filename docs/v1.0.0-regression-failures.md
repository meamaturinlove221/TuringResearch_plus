# v1.0.0 Regression Failures

Status: no unresolved failures.

Round: 194.

This file records failures found during the v1.0 full regression gate.

## Current Failure List

- none.

## Fixed During Round 194

- The initial full replay test scanned `split_ready/` with the generic privacy
  scanner and treated safety policy wording about excluded raw data as a
  release blocker. The test now scans `examples/public_demo/` directly and
  validates `split_ready/` through each bundle's `safety_report.md`.

## Fix Policy

If a failure appears, fix only the failing regression surface. Do not use this
round to introduce new features, broaden scope, create repositories, or change
public launch positioning.

## Non-failure Policy Wording

The following wording may appear as non-blocking policy text:

- fake/demo output must not be presented as observed evidence;
- private data must not be included;
- split repositories must not be created automatically;
- live adapters must remain opt-in.

These phrases are safety boundaries, not leaks or claims.
