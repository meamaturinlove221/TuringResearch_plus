# v0.7 Public Release Candidate Report

Status: GO WITH REVIEW.

Round 145 evaluates whether TuringResearch Plus can enter a public release
candidate state for v0.7. This is a release-candidate gate, not a release,
publish, tag, or legal approval.

## RC Gate Checklist

| Check | Status | Notes |
| --- | --- | --- |
| Full tests | PASS | Default fake/local suite passes. |
| mypy | PASS | `python -m mypy src` passes. |
| Privacy gate | PASS | Public demo privacy gate has no release blockers. |
| Compliance gate | PASS WITH REVIEW | Compliance reports risks and explicitly avoids legal advice. |
| Public demo | PASS | Expanded public demo remains fake/demo only. |
| Docs complete | PASS WITH REVIEW | v0.7 release-candidate docs and current docs index exist; maintainer review still required. |
| Secret scan | PASS | No real credential value is added by this gate. |
| Data payload scan | PASS | No data payload is added by this gate. |
| Local path scan | PASS | No private local project path is added by this gate. |
| Private model files | PASS | No private model payload file is added by this gate. |
| Fake result observed | PASS | Fake/demo outputs are not promoted to observed. |
| Old naming | PASS | Round 145 files avoid legacy project naming. |
| Live features optional | PASS | Live features remain opt-in and disabled by default. |
| Plugin loading safe | PASS WITH REVIEW | Trusted local plugin loading is metadata-only; real sandboxing remains a limitation. |
| Export optional backends | PASS | PDF/PPTX optional backend absence skips gracefully with recorded reasons. |

## Public RC Scope

v0.7 candidate materials include:

- trusted local plugin loading;
- plugin sandbox policy;
- plugin compatibility harness;
- MCP distribution polish;
- dashboard refinement;
- optional Advisor PDF/PPTX export paths;
- export quality gate;
- dataset / license compliance assistant;
- local-first vault UI;
- paper deep review mode;
- VGGT public case study builder;
- public demo expansion;
- v0.7 full fake replay.

## Verification Commands

```powershell
python -m pytest -q
python -m mypy src
python -m pytest tests/contract/test_name_integrity.py -q
python -m pytest tests/unit/test_privacy_models.py tests/unit/test_privacy_policy.py tests/unit/test_privacy_scanner.py tests/unit/test_redaction_rules.py tests/unit/test_privacy_report.py tests/workflow/test_public_demo_privacy_gate.py -q
python -m pytest tests/unit/test_compliance_models.py tests/unit/test_license_registry.py tests/unit/test_dataset_registry.py tests/unit/test_model_registry.py tests/unit/test_compliance_risk_checker.py tests/workflow/test_vggt_compliance_fake.py -q
```

## Decision

`GO WITH REVIEW`

The repository can prepare public release-candidate materials after maintainer
review of branch cleanliness, license posture, release notes, package metadata,
and public examples. It should not be published directly from the current dirty
working tree.
