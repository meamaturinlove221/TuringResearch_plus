# v0.7 Go / No-Go

Decision: GO WITH REVIEW.

## Go Conditions Met

- Default fake/local test suite passes.
- `python -m mypy src` passes.
- Name integrity passes.
- Privacy gate passes for public demo material.
- Compliance gate records risks without legal conclusions.
- Public demo expansion remains fake/demo only.
- Plugin loading remains trusted/local and review-gated.
- Sandbox policy blocks code execution and secret access by default.
- Optional PDF/PPTX export backends skip gracefully.
- v0.7 full fake replay passes.

## Review Required Before Public Release

- Maintainer approval of release branch state.
- License posture confirmation.
- README and release note review.
- Package metadata review.
- Public example review.
- Security policy and issue template review.

## No-Go Conditions

Do not publish if any of these are true:

- test suite fails;
- mypy fails without documented exception;
- privacy gate has release blockers;
- public demo contains private material;
- fake/demo output is marked observed;
- plugin loading executes unknown code;
- live features are enabled by default;
- optional export backend failure breaks default tests;
- release branch includes unrelated dirty worktree changes.

## Current Result

The gate result is `GO WITH REVIEW`, not automatic publication.
