# v0.6 Release Blockers

Status: blockers documented.

## Blocking For Public Release

- Current worktree contains many historical uncommitted and untracked changes.
- Current branch is not a release branch.
- v0.6 release notes and package version bump are not complete in this round.
- Public release still needs maintainer approval.
- License posture remains review-required.

## Non-blocking But Known Limitations

- Live adapters are optional and disabled by default.
- Remote artifact sync is safety-gated and not live by default.
- Plugin system is manifest-only and does not execute third-party code.
- Paper assembly remains scaffold/gate only, not final paper generation.
- Benchmark replay checks local expected outputs, not performance.
- Contract drift gate is a boundary/presence proxy, not a full semantic diff.

## Safety Blockers If Detected

- Real secrets or tokens.
- Raw data payloads.
- SMPL-X model files.
- `local_project_links.yaml`.
- Fake result marked observed.
- Live tests required by default.
- Old project naming in current docs/code.
- SparseConv3D success claim without real evidence.
