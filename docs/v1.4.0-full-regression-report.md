# v1.4.0 Full Regression Report

Status: full regression passed with review.

Round: 326.

This report records the v1.4 full regression gate across the v1.3 baseline and
the v1.4 original repo production parity additions.

## Regression Decision

Decision: `PASS WITH REVIEW`.

The v1.4 release-candidate surface is coherent enough for human release review.
It is not released, not tagged, and not published. Human review is still
required before any release branch push, GitHub release, package publication,
or child repository creation.

## Coverage

| Surface | Regression coverage |
| --- | --- |
| v1.3 workflows | v1.3 full original parity replay, public demo, release contracts, and security/privacy surfaces remain present. |
| Session production parity | CLI, script export, archive hardening, remote dry-run plan, return confirmation, E2E replay, dashboard v2, and gate remain fake/default and review-only. |
| Scholar production parity | Tool list, `paper_content` E2E, `paper_reference` E2E, three-pass reading E2E, optional backend slot, and gate remain fake/default. |
| Web production parity | URL normalization, cache manifest, content fixtures, Apify fake/live report, and gate remain fake/default with live disabled. |
| yogsoth production parity | Campaign trace, Research Catalog, Vault Wiki, Ontology, Stress / Convergence, Experiment Runbook E2E, and gate remain deterministic review workflows. |
| production dashboard/docs | Original Repo Parity Dashboard v2 and README/docs production polish remain present. |
| security/privacy | v1.4 security, privacy, and secret scan gates pass with review. |
| ARIS deferred | ARIS remains reference-only and outside v1.4 implementation. |

## Verification

| Check | Result |
| --- | --- |
| Full pytest | passed |
| `python -m mypy src` | passed |
| Name integrity | passed |
| Privacy gate | passed |
| v1.4 release contracts | passed |
| v1.4 full production replay | passed |
| v1.4 security/privacy gate | passed |
| Pre-push scan | passed with documented non-blocking safety wording |

## Fixed During Regression

- None.

## Safety Notes

- No network access was required for the public/fake regression path.
- No automatic GitHub release was created.
- No automatic PyPI publish was added.
- No automatic child repository creation was performed.
- No live tests were enabled by default.
- No automatic experiment execution, remote command execution, Modal call, GPU
  call, unknown plugin execution, or ARIS runtime feature was added.
- No private data, raw data, private local paths, or restricted model payloads
  are included in the v1.4 production parity public surfaces.
- Fake/demo outputs remain fake/demo and must not be presented as observed
  evidence.
- No SparseConv3D success claim is made.

## Carryover Items

- The current working branch still needs maintainer-managed branch cleanup.
- Public release remains a human action.
- Optional live provider and live SSH/SFTP behavior remains disabled by default.
- ARIS study remains deferred to a separate scope lock and safety review.
