# v1.4.0 Handoff

Status: final handoff.

Round: 329.

This handoff is for the next human reviewer or future development session. It
summarizes what is ready, what is not ready, and what requires human approval.

## Branch Status

Current local branch observed during Round 329:

- current branch: `pre-rename-round38-checkpoint`;
- local `dev/mainline`: present;
- local `release/v1.4.0-rc`: not present.

Because the working tree is historically dirty, a human should create or check
out a clean release branch before any v1.4 publication action.

## Original Repo Production Parity Status

Original repo production parity is ready for human review.

- Session production parity: fake/default production path is gated and
  dashboarded; live remote orchestration remains NO-GO by default.
- Scholar production parity: fake/default content/reference/reading E2E paths
  are gated; MinerU, OCR, automatic download, and fake citation verification
  remain out of scope.
- Web production parity: URL normalization, cache manifest, fixtures, and Apify
  fake/live report are gated; default live network and private scraping remain
  out of scope.
- yogsoth production parity: deterministic E2E workflows are gated; autonomous
  agent runtime and automatic experiment execution remain out of scope.
- Parity Dashboard v2: public-safe status view is available.

## ARIS Status

ARIS remains deferred from v1.4.

v1.5 planning keeps ARIS as future study only:

- `docs/v1.5.0-roadmap.md`;
- `docs/v1.5.0-candidates.md`;
- `docs/v1.5.0-aris-still-deferred.md`;
- `docs/v1.5.0-non-goals.md`;
- `docs/v1.5.0-sprint-1-recommendation.md`.

ARIS should not start in v1.5 as a default implementation. The current
recommendation is maybe v1.6 study-only after a separate scope lock.

## Test Status

Most recent recorded v1.4 gates:

- full pytest: passed in Round 326;
- `python -m mypy src`: passed in Round 326;
- name integrity: passed in Round 328 checks;
- privacy/security gate: passed with review in Round 325;
- full production replay: passed with review in Round 322;
- v1.4 release contracts: passed in Round 327 checks;
- release prep docs/version smoke: passed in Round 327.

Round 329 final smoke is a handoff check, not a replacement for clean release
branch CI.

## Release Status

v1.4 release docs are drafted:

- release notes;
- feature list;
- known limitations;
- test summary;
- upgrade guide;
- GitHub release draft;
- changelog;
- version metadata: `1.4.0rc0`.

No tag, GitHub release, PyPI package, child repository, or live provider action
has been created.

## Next Human Actions

1. Review README and v1.4 release notes together.
2. Review Original Repo Parity Dashboard v2.
3. Review security/privacy reports and secret scan report.
4. Review release contract and full regression reports.
5. Create or check out a clean `release/v1.4.0-rc` branch if release is
   approved.
6. Re-run full tests and `python -m mypy src` on that clean branch.
7. Decide whether to tag.
8. Decide whether to create a GitHub release or PyPI package.
9. Decide whether v1.5 begins with public docs deployment or release packaging.
10. Keep ARIS out of default v1.5 implementation unless a later scope lock
    explicitly says otherwise.

## Hard Stops

Do not launch if any of these are true:

- tests fail on the clean release branch;
- mypy fails without documented exception;
- name integrity fails;
- security/privacy gate fails;
- public demo includes private data;
- fake/demo output is presented as verified live research work;
- ARIS study items are presented as v1.4 implementation;
- release branch includes unrelated historical work;
- maintainer approval is missing.
