# v0.4.0 Integration Report

Status: integration gate passed for fake/default workflows.

Round: 89.

## Scope

This integration gate covers the v0.4 additions:

- Remote Artifact Sync
- Modal Run Dashboard
- Run Comparison
- Paper Digest
- Advisor Markdown Export

The gate is review-first and fake/default only. It does not run live adapters,
Modal, VGGT, remote commands, binary export converters, or private local scans.

## A. Remote Artifact Flow

Checked flow:

`GitHub / SFTP / Shared Store / Object Index -> UnifiedRemoteArtifactReport -> downstream review inputs`

Result: pass.

The fake/default workflow normalizes GitHub Artifact Sync, SSH/SFTP Remote
Reader, NAS/SMB Shared Store, and Cloud Object Artifact Index into
`UnifiedRemoteArtifactReport`. Selected artifacts become proposed imports.
Unsafe or large files remain omitted or metadata-only. No Evidence Ledger entry
is overwritten.

## B. Dashboard Flow

Checked flow:

`RunIngestReport -> ModalRunDashboard -> RunComparison`

Result: pass.

The Modal SparseConv3D fixture remains `real_backend_missing`. The dashboard
does not run Modal or VGGT. Run Comparison keeps the route in
not-enough-evidence territory and requires real sparse backend evidence before
any SparseConv3D success comparison.

## C. Paper Flow

Checked flow:

`PaperDigest -> MethodCard -> RelatedWorkPositioning -> CollisionRisk`

Result: pass.

Paper Digest fixtures remain `fake-or-manual-note` and
`requires-real-paper-review`. Method cards require human review. Related Work
Positioning and Collision Risk outputs remain conservative and do not generate
final paper claims.

## D. Advisor Flow

Checked flow:

`Evidence + Artifact + Visual + Dashboard + Paper -> Advisor Markdown Bundle`

Result: pass.

The advisor export layer builds Markdown source material only. No PDF, PPTX,
DOCX, or HTML binary export is generated. Figure and table lists are source
checklists, not fabricated charts.

## E. VGGT Flow

Checked flow:

`Research Knowledge Pack -> Route Pack -> Run Ingest -> Dashboard -> Advisor Bundle`

Result: pass.

The VGGT route remains planned / requires-real-experiment where appropriate.
The research knowledge pack retains `no_sparseconv3d_success_claim`, and the
advisor source bundle preserves planned-vs-observed boundaries.

## Gate Checks

- Default tests do not require network.
- Live tests are not part of the default integration flow.
- No private VGGT path is read.
- Modal is not executed.
- VGGT is not executed.
- PDF/PPTX export is not generated.
- Secrets are not bundled.
- Legacy project naming is not reintroduced.
- Fake results are not marked observed.
- SparseConv3D success is not claimed without real backend evidence.

## Result

GO WITH REVIEW.

v0.4 fake/default integration is coherent and suitable for maintainer review.
It is not a live remote artifact sync proof, not a Modal experiment result, and
not a paper-writing result.
