# v1.1.0 Full Regression Report

Status: full regression passed with review.

Round: 228.

This report records the v1.1 full regression gate across the v1.0 baseline and
the v1.1 additions: split-ready bundles, docs site, local server dashboard,
Dashboard Data API, paper writing beta, public demo cases, case gallery, GitHub
Actions hardening, privacy/security gates, plugin safety, and old v1.0
workflows.

## Regression Decision

Decision: `PASS WITH REVIEW`.

The v1.1 release-candidate surface is coherent enough for human release review.
It is not released, not tagged, and not published. Human review is still
required before any release branch push, GitHub release, PyPI publish, or child
repository creation.

## Coverage

| Surface | Regression coverage |
| --- | --- |
| Main repo entry | README, quickstart, MCP fake defaults, namespace imports |
| Split-ready bundles | `split_ready/turingresearch-vggt-case` and `split_ready/turingresearch-examples` |
| Docs site | static docs-site builder and docs/dashboard integration |
| Local server dashboard | localhost-only read-only routes |
| Dashboard Data API | public demo JSON export and dashboard rendering support |
| Paper writing beta | draft package, unsafe claim guard, citation guard |
| Demo cases | four v1.1 public-safe demo cases |
| Case gallery | manifest loader and Markdown renderer |
| CI workflow files | CI, docs-check, and privacy-gate workflow contract tests |
| Privacy/security | public demo privacy gate and v1 security/privacy gate |
| Plugin safety | execute-code and secrets-access permissions denied |
| Old v1.0 workflows | v1 release contracts, v1 full fake replay, public launch RC |

## Verification

| Check | Result |
| --- | --- |
| Full pytest | passed |
| `python -m mypy src` | passed |
| Name integrity | passed |
| Privacy gate | passed |
| v1.1 full fake replay | passed |
| v1.1 release contracts | passed |
| GitHub Actions workflow config tests | passed |
| Pre-push scan | passed with documented non-blocking safety wording |

## Safety Notes

- No network access was used.
- No automatic GitHub release was created.
- No automatic PyPI publish was added.
- No automatic child repository creation was performed.
- No live tests were enabled.
- No private data, raw data, local private paths, or restricted model payloads
  are included in the v1.1 public demo surfaces.
- Fake/demo outputs remain fake/demo and must not be presented as observed
  research evidence.
- SparseConv3D success is not claimed.

## Carryover Items

- The current working branch still needs maintainer-managed branch cleanup.
- Public release remains a human action.
- Physical split repositories remain manual, post-review actions.
- v1.1 docs/dashboard/paper-demo additions remain beta/review surfaces, not
  a SaaS, cloud service, or automated research system.
