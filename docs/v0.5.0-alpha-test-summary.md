# v0.5.0 Alpha Test Summary

Status: release prep.

## Test Groups

- Unit tests
- Contract tests
- Workflow tests
- Name integrity tests
- Package import tests
- Public import surface tests
- v0.5 alpha integration tests

## Required Checks

- `python -m pytest -q`
- `python -m mypy src`
- focused `ruff check` on release prep files
- package version import check
- legacy-name scan on release prep files
- secret/private-path scan on release prep files

## Current Result

Recorded in Round 99 summary after the validation run.

## Live Tests

Live tests remain skipped by default. v0.5 alpha does not require network,
provider credentials, Modal, VGGT, or remote sync.
