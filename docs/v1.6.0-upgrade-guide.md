# v1.6.0 Upgrade Guide

Status: release-candidate upgrade guide.

Round: 387.

## Version

The v1.6 release-candidate version is `1.6.0rc0`.

## Install

Use the main TuringResearch repository as the install target:

```bash
python -m pip install -e .[dev]
```

For MCP-oriented local checks:

```bash
python -m pip install -e .[dev,mcp]
```

## Import Paths

The compatibility namespace remains available:

```python
import turing_research_plus
```

Facade namespaces remain importable:

```python
import turing_research_core
import turing_research_paper
import turing_research_artifact
import turing_research_experiment
import turing_research_dashboard
import turing_research_plugins
import turing_research_cases
```

## v1.6 Additions

- Use `docs-site/release_bundle/` and
  `docs-site/release_bundle_manifest.yaml` for local docs release review.
- Use `.github/workflows/docs-pages-dry-run.yml` as a dry-run GitHub
  Pages-ready workflow draft.
- Use `split_manual/` for human-only split repository creation packs.
- Use optional live smoke docs for private Scholar, Web/Apify, and SFTP
  opt-in policy.
- Use live output redaction docs before publishing any future private live
  output.
- Use `assets/screenshots/SCREENSHOT_MANIFEST.yaml` for screenshot capture
  planning without fake screenshots.
- Use `docs/public-launch-checklist-v1.6.md` before any public launch action.
- Treat ARIS docs as deferred study material, not implemented runtime behavior.

## Behavioral Notes

- Public deployment remains manual.
- Child repository creation remains manual.
- Live adapters remain optional and disabled by default.
- Live SSH/SFTP remains opt-in.
- Remote command execution remains rejected by default.
- Release publication remains manual.
- PyPI publication remains manual.
- Package rename remains undecided.
- Public demo output remains fake/demo.
