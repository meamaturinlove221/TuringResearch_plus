# Context Pack Fixture

Status: fake/demo only.

This fixture is used by the local context pack builder runtime tests. It
contains safe Markdown/YAML inputs plus intentionally unsafe files that must be
excluded from the generated pack.

No live network, remote command, private dataset, model payload, or observed
experiment result is required.
