# Citation Safety Report



| Citation | Source status | Citation grade | Requires review |

| --- | --- | --- | --- |

| `humanram-fixture` | `fake-or-manual-note` | `false` | `true` |

| `neuralbody-fixture` | `fake-or-manual-note` | `false` | `true` |

| `hart-review-needed` | `requires-real-paper-review` | `false` | `true` |

| `vggt-hpe-review-needed` | `requires-real-paper-review` | `false` | `true` |

| `hggt-review-needed` | `requires-real-paper-review` | `false` | `true` |

| `fus3d-review-needed` | `requires-real-paper-review` | `false` | `true` |



## Unsafe Citation Uses



- `humanram-fixture` is `fake-or-manual-note` and cannot be used as a final citation.

- `neuralbody-fixture` is `fake-or-manual-note` and cannot be used as a final citation.

- `hart-review-needed` is `requires-real-paper-review` and cannot be used as a final citation.

- `vggt-hpe-review-needed` is `requires-real-paper-review` and cannot be used as a final citation.

- `hggt-review-needed` is `requires-real-paper-review` and cannot be used as a final citation.

- `fus3d-review-needed` is `requires-real-paper-review` and cannot be used as a final citation.



## Missing Review



- `humanram-fixture` requires human review before paper text.

- `neuralbody-fixture` requires human review before paper text.

- `hart-review-needed` requires human review before paper text.

- `vggt-hpe-review-needed` requires human review before paper text.

- `hggt-review-needed` requires human review before paper text.

- `fus3d-review-needed` requires human review before paper text.



## Boundary



- Fake fixtures are not final citations.

- No citation is fabricated.

- Human review is required before camera-ready paper text.
