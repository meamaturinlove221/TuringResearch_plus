# Artifact Plan

Required future artifacts:

- sparse backend probe log;
- prediction summary;
- visual board inventory;
- sha256 manifest;
- cleanup report.

The workspace index does not import these artifacts automatically.
