# Campaign Execution Trace Demo

This is a public-safe fake execution trace demo.

It is not an agent runtime. It does not execute tools, does not call an LLM,
does not use network, and does not mutate Evidence Ledger.

Files:

- `trace.md`: rendered fake campaign execution trace.

The demo shows how a routed campaign can produce a reviewable trace while
keeping all outputs proposed-only.
